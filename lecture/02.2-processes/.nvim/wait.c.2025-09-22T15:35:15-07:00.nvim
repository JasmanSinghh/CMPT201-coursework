#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();


  if (pid) {
    int wstatus = o;


    printf("Child \n");
    char *args[] = {"/bin/ls", "-a", "-l", NULL};
    execv("/bin/ls", args);
  } else {
    printf("Parent \n");

  }

  printf("end\n");
}
