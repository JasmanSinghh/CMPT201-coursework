#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();
  if (pid == 0) {
    printf("Child \n");
  } else {
    printf("Parent \n");
    execl("/bin/ls", "/bin/ls", "-a", NULL);
  }

  printf("end\n");
}
