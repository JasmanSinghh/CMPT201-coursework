#include <stdio.h>
#include <unistd.h>

int main() {

  printf(getpid(, getppid()));

  pid = fork()

      if (pid == 0) {
    printf("Child: PID=%d, parent PID%d\n, getpid(), getppid()")
  }
  else {
    printf("")
  }
